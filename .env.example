# ============================================
# 应用核心配置
# ============================================

# 访问密码（必填）
# 用于登录应用的密码，建议使用强密码（至少 16 位，包含大小写字母、数字和特殊字符）
APP_PASSWORD=your_secret_password_here

# 站点域名（必填）
# 用于生成二维码中的完整 URL，必须是可访问的完整域名（包含协议）
# 示例：https://filament.example.com 或 http://192.168.1.100:3000
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# ============================================
# 数据库配置
# ============================================

# 数据库连接字符串
# Docker 部署：使用 file:/app/data/spool_tracker.db（与 Volume 挂载路径对应）
# 本地开发：使用 file:./prisma/dev.db
DATABASE_URL=file:/app/data/spool_tracker.db

# ============================================
# 安全配置
# ============================================

# Token 签名密钥（可选）
# 用于 JWT token 签名，不设置则使用 APP_PASSWORD
# 建议在生产环境中设置独立的密钥以提高安全性
# 生成方法：openssl rand -base64 32
TOKEN_SECRET=

# ============================================
# 运行环境配置（可选）
# ============================================

# Node.js 环境
# 可选值：development, production, test
# Docker 部署时会自动设置为 production
# NODE_ENV=production

# 服务端口（可选）
# Docker 部署时默认为 3000，如需修改需同步更新 docker-compose.yml
# PORT=3000

# 主机名（可选）
# Docker 部署时默认为 0.0.0.0，允许外部访问
# HOSTNAME=0.0.0.0

# 禁用 Next.js 遥测（可选）
# 设置为 1 可禁用 Next.js 的匿名使用数据收集
# NEXT_TELEMETRY_DISABLED=1
